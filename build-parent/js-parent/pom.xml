<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>me.chrisswr1</groupId>
		<artifactId>build-parent</artifactId>
		<version>0.1.0-SNAPSHOT</version>
	</parent>

	<artifactId>js-parent</artifactId>
	<packaging>pom</packaging>

	<name>JavaScript Parent</name>
	<description>
		Defines the basic process to build JavaScript artifacts for npm.
	</description>

	<properties>
		<project.module-root.relativedir>${project.parent.relativedir}../</project.module-root.relativedir>

		<nodejs.version>11.1.0</nodejs.version>
		<yarn.version>1.12.3</yarn.version>
		<npm-typescript.version>3.1.6</npm-typescript.version>
		<ts-node.version>7.0.1</ts-node.version>
		<tslint.version>5.11.0</tslint.version>
		<codelyzer.version>4.5.0</codelyzer.version>
		<core-js.version>2.5.7</core-js.version>
		<rxjs.version>6.3.3</rxjs.version>
		<zonejs.version>0.8.26</zonejs.version>
		<angular.version>7.0.3</angular.version>
		<grunt.version>1.0.3</grunt.version>
		<grunt-cli.version>1.3.2</grunt-cli.version>
		<grunt-tslint.version>5.0.2</grunt-tslint.version>
		<grunt-ts.version>5.5.1</grunt-ts.version>
		<uglify.version>4.0.0</uglify.version>
		<angular.cli.version>7.0.5</angular.cli.version>
		<mocha.version>5.2.0</mocha.version>
		<chai.version>4.2.0</chai.version>
		<karma.version>3.1.1</karma.version>
		<karma.mocha.version>1.3.0</karma.mocha.version>
		<karma.chai.version>0.1.0</karma.chai.version>
		<karma.chrome.version>2.2.0</karma.chrome.version>
		<karma.firefox.version>1.1.0</karma.firefox.version>
		<karma.opera.version>1.0.0</karma.opera.version>
		<karma.safari.version>1.0.0</karma.safari.version>
		<karma.edge.version>0.4.2</karma.edge.version>
		<karma.ie.version>1.0.0</karma.ie.version>
		<karma.mocha.reporter.version>2.2.5</karma.mocha.reporter.version>

		<plugin.frontend.version>1.6</plugin.frontend.version>
	</properties>

	<build>
		<sourceDirectory>src/main/ts</sourceDirectory>
		<testSourceDirectory>src/test/ts</testSourceDirectory>
		<outputDirectory>${project.build.directory}/dist</outputDirectory>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
			<resource>
				<directory>${project.build.directory}/generated-resources</directory>
				<includes>
					<include>project.properties</include>
				</includes>
				<filtering>true</filtering>
			</resource>
			<resource>
				<directory>${project.basedir}</directory>
				<includes>
					<include>README.md</include>
				</includes>
			</resource>
		</resources>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.portals.jetspeed-2</groupId>
					<artifactId>jetspeed-unpack-maven-plugin</artifactId>
					<executions>
						<execution>
							<id>unpack-js-project-files</id>
							<phase>initialize</phase>
							<goals>
								<goal>unpack</goal>
							</goals>
							<configuration>
								<unpack>
									<artifact>me.chrisswr1:general-resources:jar</artifact>
									<targetDirectory>${project.basedir}</targetDirectory>
									<resources>
										<resource>
											<path>resources</path>
											<include>tslint.json</include>
											<overwrite>true</overwrite>
											<flat>true</flat>
										</resource>
									</resources>
								</unpack>
							</configuration>
						</execution>
						<execution>
							<id>unpack-unfiltered-js-project-files</id>
							<phase>initialize</phase>
							<goals>
								<goal>unpack</goal>
							</goals>
							<configuration>
								<unpack>
									<artifact>me.chrisswr1:general-resources:jar</artifact>
									<targetDirectory>${project.build.directory}/generated-resources</targetDirectory>
									<resources>
										<resource>
											<path>unfiltered-resources</path>
											<include>tsconfig.json</include>
											<flat>true</flat>
										</resource>
									</resources>
								</unpack>
							</configuration>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<artifactId>maven-resources-plugin</artifactId>
					<executions>
						<execution>
							<id>generate-js-project-files</id>
							<phase>generate-sources</phase>
							<goals>
								<goal>copy-resources</goal>
							</goals>
							<configuration>
								<resources>
									<resource>
										<directory>${project.build.directory}/generated-resources</directory>
										<includes>
											<include>tsconfig.json</include>
										</includes>
										<filtering>true</filtering>
									</resource>
									<resource>
										<directory>src/main/node</directory>
										<includes>
											<include>package.json</include>
											<include>Gruntfile.js</include>
										</includes>
										<filtering>true</filtering>
									</resource>
								</resources>
								<outputDirectory>${project.basedir}</outputDirectory>
							</configuration>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>com.github.eirslett</groupId>
					<artifactId>frontend-maven-plugin</artifactId>
					<version>${plugin.frontend.version}</version>
					<configuration>
						<installDirectory>build-exe</installDirectory>
						<nodeVersion>v${nodejs.version}</nodeVersion>
						<yarnVersion>v${yarn.version}</yarnVersion>
					</configuration>
					<executions>
						<execution>
							<id>install-nodejs</id>
							<goals>
								<goal>install-node-and-yarn</goal>
							</goals>
						</execution>
						<execution>
							<id>yarn-install</id>
							<goals>
								<goal>yarn</goal>
							</goals>
						</execution>
						<execution>
							<id>grunt-build</id>
							<goals>
								<goal>grunt</goal>
							</goals>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>com.github.eirslett</groupId>
				<artifactId>frontend-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>
